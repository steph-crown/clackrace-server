# Single-package project — `packages` is required when this file exists
# (Railway: ERROR packages field missing or empty).
packages:
  - "."

# pnpm 11+: allow esbuild postinstall for tsx/drizzle-kit/vitest.
allowBuilds:
  esbuild: true
